import os
import torch
import logging
import numpy as np
from builtins import object

# Brain mask computation


def cal(raw_perf, device):

    print('Masking raw data ...')

    a

    return mask

